[package]
name = "yahoo_fantasy"
version = "0.1.0"
edition = "2024"

[dependencies]
oauth2 = "5.0"
quick-xml = "0.38"
serde-xml-rs = "0.8.1"
serde = { version = "1.0", features = ["derive"] }

utils = { path = "../utils", features = ["fantasy"] }